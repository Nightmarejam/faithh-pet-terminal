# Cursor / Windsurf session context

**→ Read [`AGENTS.md`](AGENTS.md) at the repo root. That is the only live context document.**

This file used to carry its own copy of the project state, which meant it drifted out of sync
with five other "master" docs. It is now a pointer on purpose. Do not add content here — put it
in `AGENTS.md` so there is exactly one thing to keep true.
