# FAITHH System Status — 2026-03-07

## Confirmed Working

**Backend**
- Flask v4.0-pulse on port 5557, threaded=True
- ChromaDB on Gen8 (servicebox.taileb8c60.ts.net:8000) — 38,335 documents
- Groq, Gemini, Ollama (qwen25-grounded:latest default) — all healthy
- 15 ML macro-chips loaded, cosine routing active
- PULSE Reflection Engine — 3 tiers operational
- Coherence Arbiter — 82.5% anchor validation score

**UI — faithh_pet_v4.html (verified post-crash, file intact)**
- ✅ Retry button on failed messages (lastFailedMessage + retryLastMessage())
- ✅ deepseek-r1:32b disabled + greyed with ⚠️ BROKEN (sm_61 CUDA) label
- ✅ Chat persistence, coherence indicator, RAG sources panel, Program Advance

**RAG / Knowledge Base**
- 38,335 chunks indexed (harmony/IME docs included)
- Phase 4 content present in first chunks of all core orientation docs
- Orientation queries return correct phase

**Documentation (all updated this session)**
- SYSTEMS_MAP.md — Phase 4, phase_history block, chunk count corrected
- CONTEXT.md — regenerated, Phase 4 Active
- project_states.json — Phase 4 Active
- scaffolding_state.json — Phase 4 Active, session summary current
- decisions_log.json — infra_002 (WSL crash rule) added
- .windsurf/rules/faithhprojectspecifics.md — WSL crash + script writing rules added


## Not Working / Degraded

**WSL Stability — most urgent issue**
Crashes repeatedly. Three known triggers:
1. Any WSL script importing torch or SentenceTransformer (sm_61 CUDA incompatibility)
2. Large ChromaDB upserts without pre-chunking
3. Scripts written via PowerShell heredoc (corrupts newlines, causes hangs at runtime)

Recovery is ~10s and is clean — backend restarts fine, ChromaDB on Gen8 is never
at risk. But the crashes interrupt workflow and make scripting unreliable.

Immediate mitigations to try (in order):
1. Create `C:\Users\jonat\.wslconfig` with adequate headroom (current runbook: **memory=48GB**, **swap=32GB** — see `runbook-to-rule-them-all/runbooks/entries/FAITHH_Runbook_v1.md`). Older notes here suggested `memory=24GB`; treat that as historical.
   Then: `wsl --shutdown`, restart WSL
2. Fix /etc/wsl.conf line 4 — has unknown key 'boot.systemdTimeout' causing parse warning
3. For all future script writing: use Desktop Commander write_file or faithh-filesystem
   MCP — never PowerShell heredoc

**Full Core Doc Reindex — Partial only**
Only first ~1500 chars of SYSTEMS_MAP.md, CONTEXT.md, scaffolding_state.json,
project_states.json were indexed. Phase 4 lands in the first chunk of each so
orientation queries work, but full content still missing from RAG.
Fix: Windsurf task — rewrite reindex_core_docs_v2.py via Desktop Commander, then run.

**deepseek-r1:32b**
Disabled in UI. Still present in Ollama but will CUDA crash if invoked directly.
Remove when WSL is stable: ollama rm deepseek-r1:32b

---

## On Dual Boot

Not necessary yet. The crashes are pattern-driven, not fundamental WSL instability.
Three rules prevent them. Recovery is fast and data is never at risk (ChromaDB is on Gen8).

Dual boot would help if: GPU passthrough to WSL2 is genuinely broken for this hardware
config (sm_61 + RTX combo), or if you need native Linux for audio production.
Neither is clearly the case. Ensure `.wslconfig` RAM/swap match current hardware (see runbook).
If crashes continue after that with no torch imports involved, then revisit.

---

## Open Loops (unchanged)
- Journal synthesis script (HANDOFF_JOURNAL_SYNTHESIS_2026-02-25.md)
- Form 1065 — pending CPA
- Kevin Dunn exit — withdrawal agreement unsigned
- FGS — 6 triggers unmet
