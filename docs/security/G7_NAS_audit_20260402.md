# G7 NAS Audit — 2026-04-02

**Host:** `ISaidGoodDay` (Synology DS220j, `synology_rtd1296_ds220j`)  
**Access:** SSH `nas` (key auth) from WSL — **working**

## Host identity

```
ISaidGoodDay
Linux ISaidGoodDay 4.4.302+ #86009 SMP Wed Nov 26 18:28:01 CST 2025 aarch64 GNU/Linux synology_rtd1296_ds220j
```

## Users with shell access

```
admin:x:1024:100:System default user:/var/services/homes/admin:/bin/sh
daemon:x:2:2::/:/bin/sh
lp:x:7:7::/var/spool/lpd:/bin/sh
Nightmarejam:x:1026:100::/var/services/homes/Nightmarejam:/bin/sh
postgres:x:55:55::/var/services/pgsql:/bin/sh
root:x:0:0::/root:/bin/ash
```

## Listening ports

```
tcp        0      0 0.0.0.0:139             0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:80              0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:22              0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:65368           0.0.0.0:*               LISTEN      -                   
tcp        0      0 127.0.0.1:5432          0.0.0.0:*               LISTEN      -                   
tcp        0      0 127.0.0.1:33304         0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:443             0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:445             0.0.0.0:*               LISTEN      -                   
tcp        0      0 127.0.0.1:161           0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:6690            0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:5000            0.0.0.0:*               LISTEN      -                   
tcp        0      0 0.0.0.0:5001            0.0.0.0:*               LISTEN      -                   
tcp6       ...    :::139,80,22,443,445,3261,3263,3264,6690,5000,5001 ...
```

*(Full `netstat` output omitted duplicate 80/443/5000/5001 listeners — typical DSM multi-service binding.)*

## Recent auth events

Log discovery (`find /var/log ...`) matched package logs; standard `auth.log` / `synolog/connection.log` tail not available to SSH user without elevated paths.

```
/var/log/dpkg_upgrade.log
/var/log/packages/ActiveInsight.log
/var/log/packages/OAuthService.log
/var/log/packages/ProxyServer.log
/var/log/dpkg.log
```

**Recommendation:** Review DSM **Log Center** and SSH login history as admin in DSM UI.

## Installed packages (sample)

Via `/usr/syno/bin/synopkg list` (first 40 lines of output):

- FileStation, CloudSync, SynologyPhotos, SMBService, SynologyDrive, QuickConnect, Tailscale, ActiveInsight, Node.js_v20, git, SecureSignIn, rclone, Python2, python312, synocli-*, StorageManager, ScsiTarget (SAN Manager), SynoFinder, etc.

## node_exporter status

```
(no matching process)
```

`curl http://127.0.0.1:9100/metrics` — **not responding** (node_exporter not running on this NAS).

## Risk flags

| Item | Notes |
|------|--------|
| Port **65368** | **Unidentified** from non-root `netstat` (PID column `-`). Confirm in DSM **Resource Monitor → Network** or `sudo lsof -i :65368` as admin. Often a package auxiliary listener (e.g. cloud/sync/insight); treat as review until mapped. |
| Port **6690** | Synology Drive — **expected** if Drive Server is used. |
| **5000 / 5001** | DSM HTTP/HTTPS — **expected**; ensure DSM firewall + LAN-only if possible. |
| **445 / 139** | SMB — **expected** for NAS role; restrict to LAN in DSM firewall. |
| **postgres 127.0.0.1:5432** | Local only — **acceptable**. |
| **QuickConnect** | Installed — review whether WAN exposure is required vs Tailscale-only. |
| **Tailscale** | Installed — aligns with private overlay access. |

## Result: **PARTIAL**

SSH audit succeeded; high-port **65368** and auth log depth need admin follow-up in DSM.
