# G6 UDM Audit — 2026-04-02

## Access and identity

- Hostname: `PootyTang`
- Firmware: `v5.0.16`
- SSH path: `WSL -> ProxyJump gen8 -> root@192.168.1.1`
- Status: **key-based SSH fixed and confirmed**

Verification:

```text
ssh -o IdentitiesOnly=yes -i ~/.ssh/id_ed25519 udm "uname -a"
Linux PootyTang 4.19.152-ui-alpine #4.19.152 SMP Fri Feb 27 00:39:01 CST 2026 aarch64 GNU/Linux
```

## Network topology on UDM

Interfaces/subnets observed:

- WAN: `eth4` with public IP `76.138.140.7/23`
- LAN bridge: `br0` -> `192.168.1.1/24`
- VLAN/segment: `br30` -> `192.168.10.1/24`

Routes:

- `76.138.140.0/23` via `eth4`
- `192.168.1.0/24` via `br0`
- `192.168.10.0/24` via `br30`

## Listening services (security-relevant highlights)

Open/listening ports include:

- SSH `22`
- HTTP/HTTPS `80`, `443`
- UniFi management/app ports `8080`, `8443`, `8444`, `8880`, `8843`, `6789`
- DNS/DHCP and local resolver stack (dnsmasq/coredns ports)

## NAT/port-forward findings

`iptables -t nat -S` contains DNAT/MASQUERADE entries forwarding WAN traffic to LAN host `192.168.1.15` for:

- `80`
- `443`
- `5001`
- `32400`
- `43`

Risk note:

- Verify each forward is intentional. `32400` (Plex) is common but increases exposure.
- Port `43` forwarding is unusual for home setups; validate owner/use case.

## Firewall policy findings

- UDM uses `UBIOS_*` chains and zone-aware policy hooks.
- Evidence of segmentation policy objects across LAN/DMZ/GUEST/VPN.
- Multiple predefined drop/accept hooks are present; no blanket "allow all" chain observed from sampled rules.

## Auth/log findings

- SSH authentication now works with key auth.
- System log grep for SSH-specific auth lines was noisy/inconclusive in this shell session.
- Wireless/auth churn entries are present in `/var/log/messages`.

Recommendation:

- Confirm SSH auth events in UniFi UI logs (System Log / Security events) for clearer audit trail.

## Result: PARTIAL+

What is closed:

- UDM reachable on correct gateway IP (`192.168.1.1`)
- ProxyJump routing solved
- key-based SSH validated
- NAT and firewall tables sampled and documented

What remains:

- UI-level audit completion: admin MFA verification, firmware channel/availability check, and explicit review of each configured port-forward rule with business rationale.
