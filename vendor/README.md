# vendor/

Reserved for **large third-party trees** (e.g. `llama.cpp`) when converted to submodules or documented vendored copies.

`llama.cpp/` may remain at repository root until a deliberate migration; do not duplicate without updating build scripts and docs.
