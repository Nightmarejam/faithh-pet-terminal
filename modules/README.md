# modules/

Place **import-only** shared libraries here as the Canvas architecture grows. The canonical Flask app remains `faithh_professional_backend_fixed.py` with supporting code in `backend/`.

Sidecar Flask apps live under `services/`.
