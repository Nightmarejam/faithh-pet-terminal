# legacy_ui/

Holds **retired** root-level or near-root HTML shells when the Canvas UI consolidates on `faithh_pet_v4.html` and `faithh_cockpit.html`.

As of 2026-04-07 there were **no extra `*.html` files at repository root** to move; older UIs already live under `archive/` and `llama.cpp/`.
